	
struct listen
{
    std::string ip;
    std::string port;
};

enum method_http
{
	GET,
	POST,
	DELETE,
	HEAD
}

struct request
{
	unsigned int method; (method_http)
	std::string url;
	std::string protocol
	std::string header
	std::string body
}

struct reponse
{
	std::string protocol
	unsigned int status_code;
	std::string reason-phrase;
	std::string header
	std::string body
}


Après analyse, il a toutes les infos :
	Quelle action ? → méthode
	Quelle ressource ? → URL
	Qui fait la requête ? → auth / cookies
	Quel format ? → headers
	Quelles données ? → body


👉 Il peut maintenant décider :
	autoriser ou refuser
	chercher des données
	exécuter du code
